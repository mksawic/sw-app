import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3c6edb",
    },
    secondary: {
      main: "#ffe81f",
    },
    mode: "dark",
  },
});

export default theme;
